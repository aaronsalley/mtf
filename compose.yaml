version: '3.9'

services:
  optack:
    extends:
      file: ./packages/optack/compose.yaml
      service: core
    image: ${GAR_LOCATION}-docker.pkg.dev/${PROJECT_ID}/optack:${TAG}
    restart: always
    ports:
      - '32767:3000'

  storybook:
    extends:
      file: ./packages/optack/compose.yaml
      service: storybook
    image: ${GAR_LOCATION}-docker.pkg.dev/${PROJECT_ID}/optack:${TAG}
    restart: always
    ports:
      - '32766:6006'

  jamstack:
    build: ./packages/jamstack
    image: ${GAR_LOCATION}-docker.pkg.dev/${PROJECT_ID}/mtfmusicals:${TAG}
    restart: always
    ports:
      - '32768:3000'
    networks:
      - wordpress

  web_db:
    image: mariadb
    restart: always
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: 1
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    networks:
      - wordpress

  wp:
    image: wordpress:latest
    restart: always
    ports:
      - '32769:80'
    environment:
      WORDPRESS_DB_HOST: ${DB_HOST}
      WORDPRESS_DB_NAME: ${DB_NAME}
      WORDPRESS_DB_USER: ${DB_USER}
      WORDPRESS_DB_PASSWORD: ${DB_PASSWORD}
    depends_on:
      - web_db
    networks:
      - wordpress

volumes:
  sql_data:
  wp_data:

networks:
  wordpress:
    driver: bridge
