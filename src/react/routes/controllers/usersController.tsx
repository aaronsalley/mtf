'use strict';

import logger from 'logger';

class Users {
  public createUser = (req, res) => {
    try {
    } catch (error) {
    }
  }

  public listUsers = (req, res) => {
    try {
    } catch (error) {
    }
  }
}

export default new Users();
